// ==================== ADMIN CONFIG ====================
// Single source of truth for the backend API URL.
// Update this one constant when the backend URL changes.

// Only assign to window — no const, to avoid "already declared" conflict with api files
window.API_URL = 'https://primejo-ecommerce-backend-demo.up.railway.app/api';
